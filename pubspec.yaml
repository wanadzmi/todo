name: todo
description: A TODO apps as a part of Mobile Apps Developer Assessment given by ETIQA IT

publish_to: 'none' 

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  

  # Format DateTime
  intl:

  # Database
  sqflite:

  # UI
  page_transition: ^2.0.4
  cupertino_icons: ^1.0.2
  datetime_picker_formfield: ^2.0.0
  flutter_slidable:

  

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^1.0.0

flutter:
  uses-material-design: true
